# 文档计划审计待办

更新时间：`2026-04-14 00`

说明：本文件承接 `docs/qa-report/document-plan-audit.md` 的后续讨论。后面如果继续围绕这条议题沟通，直接更新本文件，不重复新建同主题 todo。

## 当前判断

- 当前最该优先做的不是继续新增 spec，而是把 `docs/superpowers` 收口成可信真相层。
- 后端基础门禁已经站稳；前端工程门禁也已通过，但产品语义仍停留在 bootstrap/prototype。
- 当前文档问题是三类叠加：
  - 索引漏维护
  - 状态语义混用
  - 目录同级文件数超限

## 建议待办

| 优先级 | 待办 | 预期结果 | 风险 / 备注 |
| --- | --- | --- | --- |
| `P0` | 收口 `docs/superpowers/specs/1flowse/README.md` 和 `docs/superpowers/plans` 索引 | 离线阅读能直接判断当前范围和入口 | 需要先做一轮纯文档整理 |
| `P0` | 把 `modules/README.md` 改成 `design / implementation / verification` 三轴状态 | 不再把“设计确认”误读成“代码已完成” | 需要回补已有模块状态 |
| `P0` | 为 `specs/1flowse` 与 `plans/` 设计子目录或归档规则 | 两个目录都回到 `<=15` 同级文件 | 需要一次性迁移部分链接 |
| `P1` | 给 plan 状态回填建立统一规则 | 已执行计划不再继续显示 `0/x` | 可搭配轻量脚本或 checklist |
| `P1` | 为文档一致性加一个轻量守卫 | README 漏索引、目录超量、plan 未回填可前移发现 | 前期需要维护少量规则 |
| `P1` | 决策前端下一步到底是“正式控制台”还是“继续 prototype” | 前端不会继续混着推进 | 需要明确下一轮产品重点 |
| `P2` | 如果进入正式控制台路线，执行“控制台壳层、认证接入与设置区”方案 | 替换 `Bootstrap` 文案，并建立最小真实主路径 | 会占用一个完整前端迭代 |
| `P2` | 文档和壳层真相层收口后，再做路由级拆包 | 主 chunk 下降，后续扩展压力更小 | 不建议现在抢先做 |

## 当前建议

1. 先做 `P0`，不要跳过。
2. `P1` 里最值得尽早补的是“状态回填规则”和“轻量一致性守卫”。
3. 前端下一步建议二选一，不要并行：
   - `A`：进入正式控制台路线
   - `B`：继续 prototype，但隐藏未完成入口和 bootstrap 心智
4. 在 `P0/P1` 没做完之前，不建议再增加新的 `docs/superpowers` 文档平铺文件。

## 讨论记录

- `2026-04-14 00`：初版待办，由最新 QA 报告同步生成。
