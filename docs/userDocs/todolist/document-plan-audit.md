# 文档计划审计待讨论

更新时间：`2026-04-16 10:06 CST`

说明：本轮继续沿用同主题，但只保留最值得讨论的新结论，不重复上一版已经讲清的旧问题。核心新增判断有四个：`产品北极星和当前基线没分层`、`执行已经明显偏向 editor`、`前端已进入性能治理区`、`小时级审计刷新正在制造噪声`。

## 1. 现状

- 最近 `24` 小时共有 `47` 次提交，其中 `6` 次直接是在刷新同一份 `document-plan-audit`。
- 当前真实开发状态不是“03/04 待开发”，而是：
  - `03 Application 宿主基线已落地`
  - `04 agentFlow authoring baseline 已落地`
  - `05/06B` 仍没有最小外部价值闭环
- 本轮重新跑到的验证：
  - `pnpm --dir web lint`：通过，但 `node-registry.tsx` 仍有 `4` 条 warning
  - `pnpm --dir web test`：通过，`30` 个文件、`89` 个测试
  - `pnpm --dir web/app build`：通过，但主包已经到 `5.19 MB`
  - `node scripts/node/verify-backend.js`：失败，停在 `cargo fmt --check`
  - `cargo test -p api-server application_orchestration_routes`：通过
  - `cargo test -p storage-pg flow_repository_tests`：通过
- 当前最关键的新问题：
  1. `北极星产品文档` 和 `当前交付基线` 没有拆开
     - 产品设计仍强调 `发布优先`
     - 但模块总览和部分模块 README 还在把 `03/04` 说成待开发或未来设计
  2. `最近 24 小时执行重心已经明显 editor-first`
     - `agent-flow`/相关 plan/spec 触达 `101`
     - `05/06B/runtime/publish` 相关触达只有 `1`
  3. `前端已经进入性能治理区`
     - 构建主包 `5,192.02 kB`
     - 路由仍是静态页面导入，没有真正路由级懒加载
  4. `审计文档刷新开始反过来制造噪声`
     - 同一主题报告小时级反复重写，新增信息密度反而下降

## 2. 可能方向

### 方向 A：先重写真值层

- 把“北极星产品文档”和“当前交付基线”拆开
- 同步回写 `modules/README` 与 `03/04/05/06B README`

### 方向 B：下一条功能切片先补最小 `05/06B`

- 不再继续优先做 editor 小功能
- 先做一条最小对外交付证明

### 方向 C：先修官方门禁和审计节奏

- 让 `verify-backend.js` 回绿
- 把小时级 audit 改成 `delta-only`

### 方向 D：补第一轮前端性能治理

- 路由懒加载
- chunk 切分

### 方向 E：再做 `agentFlow` store-centered 第一阶段重构

- 收口 `store / transforms / hooks / adapters`
- 停止往当前 shell-centric 结构上继续堆功能

## 3. 不同方向的风险和收益

### 方向 A：重写真值层

- 收益：以后讨论进度和优先级时会回到同一事实层
- 风险：短期没有新功能截图

### 方向 B：补最小 `05/06B`

- 收益：最快把项目重新拉回 `publish-first` 身份
- 风险：如果不先控住结构和门禁，会把旧债带进新模块

### 方向 C：修门禁和审计节奏

- 收益：完成标准重新可信，审计文档重新有信号
- 风险：治理投入对外部观感不明显

### 方向 D：前端性能治理

- 收益：在 `05/06B` 继续进来之前先控住主包体积
- 风险：短期对产品主线帮助不如真值层和发布主线纠偏直接

### 方向 E：store-centered 重构

- 收益：把 `04` 从“能跑”提升到“能持续扩展”
- 风险：如果先做它，项目还是会继续停留在 authoring 视角

## 4. 对此你建议是什么？

建议顺序：`A + C -> B -> E -> D`

### 我建议现在先做

1. 把北极星产品文档和当前交付基线正式拆开。
2. 同步修正模块状态文档。
3. 让 `verify-backend.js` 回绿，并把 audit 改成 delta 模式。

### 我建议紧接着做

1. 下一条功能切片直接补最小 `05/06B` 价值证明。
2. 不要再把 editor 小功能作为默认主线。

### 我建议随后做

1. 执行 `agentFlow` store-centered 第一阶段重构。
2. 之后再补前端路由切分和包体治理。

### 当前不建议优先做

1. 继续小时级重写同一份大审计文档。
2. 继续往当前 editor 壳层里叠功能。
3. 继续把北极星 P1 文档直接当成当前现状引用。
