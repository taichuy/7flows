# User Preferences

这里记录在开发过程中确认的“长期有效用户偏好”，用于指导后续实现、文档和 skill 调整。

## 使用原则

- 只记录稳定偏好，不记录一次性任务要求。
- 偏好需要能指导未来决策，而不是仅复述聊天内容。
- 如果某条偏好已经沉淀为仓库级规则或专项 skill 规则，需要在“落地动作”里说明同步位置。

## 记录模板

```md
## YYYY-MM-DD 偏好主题

- 偏好：
- 来源：
- 影响范围：
- 落地动作：
- 备注：
```

## 当前已记录偏好

## 2026-03-09 文档与溯源优先

- 偏好：项目早期要重视用户偏好记录、skill 规范沉淀和开发过程可溯源。
- 来源：初始化项目 AGENTS 规范时的明确要求。
- 影响范围：`AGENTS.md`、`.agents/skill-cn/`、`docs/dev/`
- 落地动作：新增仓库级 `AGENTS.md`；约定稳定偏好记录到本文件；要求重要开发动作沉淀到 `docs/dev/`；要求废弃文档文件名显式包含 `expired`。
- 备注：后续若出现更细化的偏好，应继续追加，不要覆盖本条。

## 2026-03-10 文件体量控制

- 偏好：单个代码文件不应无限增长；后端代码文件原则上不超过 1500 行，前端代码文件原则上不超过 2000 行，超过时要优先评估拆分、抽离和封装，而不是继续堆叠职责。
- 来源：用户在“进行下一步开发”过程中明确提出，希望把文件行数上限沉淀为长期开发偏好。
- 影响范围：`api/`、`web/`，以及对应的 review、refactor、testing 和开发记录。
- 落地动作：已记录到 `docs/dev/user-preferences.md`；后续新增实现、代码审查和组件/模块重构时，将把文件体量作为显式检查项，并在接近或超过阈值时优先建议拆分。
- 备注：这是工程可维护性约束，不是机械性的硬性阻断规则；如果短期内因迁移或集中改造暂时超过阈值，需要尽快补上拆分计划。

## 2026-03-10 开发完成后提交 Git 代码

- 偏好：每一轮开发完成后都提交一次 Git 代码，并使用规范、可读、能概括改动范围的提交信息，方便后续开发快速理解上一轮做了什么。
- 来源：用户在本轮开发完成后的明确要求，希望把“开发闭环 = 完成实现 + 形成规范提交”沉淀为长期协作偏好。
- 影响范围：整个仓库的日常开发节奏、提交粒度、提交信息命名方式，以及后续问题追踪与上下文交接。
- 落地动作：已记录到 `docs/dev/user-preferences.md`；后续在每次完成一轮可验证开发后，默认执行一次非交互式 Git 提交，并沿用仓库当前的规范化提交风格。
- 备注：提交内容优先包含源码、测试和开发记录；默认不把 `__pycache__` 等运行产物纳入提交。

## 2026-03-10 IR 优先与兼容层旁挂

- 偏好：涉及外部生态接入时，坚持 `7Flows IR` 优先、原生生态优先，并把 Dify 等兼容能力设计为可启停的旁挂兼容层，而不是写死进核心后端。
- 来源：用户连续推进“架构设计理念”整理，明确要求继续完善这条设计方向，并已在产品文档与技术补充中形成稳定表达。
- 影响范围：`docs/product-design.md`、`docs/technical-design-supplement.md`、`docs/dev/`，以及未来插件体系、运行时、发布层和插件管理相关实现。
- 落地动作：补强产品文档中的全局设计原则；补充插件兼容章节的架构理念表述；新增插件生态架构开发记录并在后续实现中按 `native / compat:*` 分层建模。
- 备注：该偏好本质上是平台长期架构边界，不只适用于 Dify，也适用于未来其他外部生态。

## 2026-03-10 测试结果与应用日志应可见

- 偏好：测试结果应作为可见的开发信号被记录和展示，应用日志应对用户可见，便于 AI 后续维护与用户优化编排。
- 来源：用户在要求把 discovery/sync 链路接到前端与系统诊断时，明确说明“测试可见”和“应用日志可见”是长期偏好与 7Flows 期望。
- 影响范围：`docs/dev/` 开发记录、系统诊断视图、调试面板、运行态观测接口，以及后续测试与验证流程。
- 落地动作：本轮先把 plugin discovery/sync、adapter health 与最近 `run_events` 接入首页诊断区；同时将每轮验证结果继续写入开发记录，后续逐步推进更系统化的验证结果展示。
- 备注：日志可见优先复用统一运行事件流，而不是为不同调试场景各起一套日志机制。

## 2026-03-10 日志粒度要可见但不过载

- 偏好：监控和系统诊断应保持“摘要优先、详情可追溯”的日志粒度，既不能让开发功能被日志淹没，也不能在运行时制造一堆难以维护的日志噪音。
- 来源：用户在继续完善系统诊断时，明确要求参考 Dify 风格的可视化监控，但控制日志粒度与维护成本。
- 影响范围：`api/` 的系统诊断与运行态接口、`web/` 的监控与调试视图、后续 run events 展示与日志设计。
- 落地动作：把系统诊断里的近期运行事件收敛为事件类型统计、payload key 与 preview；完整 payload 继续保留给专门的 run 详情和调试链路。
- 备注：默认监控面板展示聚合信息与预览，详细日志只在需要时展开。

## 2026-03-10 工具读取中文内容默认使用 UTF-8

- 偏好：使用终端或工具读取包含中文的文件时，应默认显式使用 UTF-8，避免因编码错误看到乱码后再做错误判断或修改。
- 来源：用户指出多次工具读取未显式使用 UTF-8，导致中文内容出现乱码。
- 影响范围：文档读取、代码查看、审查、开发记录维护，以及所有需要通过工具读取中文内容的流程。
- 落地动作：后续读取中文文档或源码时优先显式使用 UTF-8；同时将该规则同步到仓库级协作约定。
- 备注：如果文件编码不是 UTF-8，应先确认实际编码，再继续分析或修改。

## 2026-03-10 AI 追溯不能只依赖前端面板

- 偏好：面向 AI 开发与自动化排障的日志追溯，不能只依赖给人看的首页或调试面板，必须保留机器可读的原始运行态事实来源，并与开发过程留痕分层。
- 来源：用户在继续补 run 诊断面板时，明确追问“AI 开发过程中日志追溯怎么看”，并要求先把规则和方案定清楚。
- 影响范围：`api/` 运行态接口、`web/` 诊断面板职责边界、`docs/dev/` 开发记录，以及后续 AI 辅助开发和自动化能力设计。
- 落地动作：新增 AI 追溯分层方案开发记录；在 `runtime-foundation` 中明确 `system overview` 与 `run detail` 的接口边界；将仓库级协作规则补充到 `AGENTS.md`。
- 备注：给人看的面板负责摘要、导航和排障入口；给 AI / 自动化 的追溯应直接面向 `runs`、`node_runs`、`run_events` 及对应 API。

## 2026-03-10 外部插件先压缩为受约束 IR

- 偏好：继续推进插件适配层时，所有外部插件定义和调用语义都必须先被压缩、规整为受约束的 `7Flows IR`，再进入平台内部执行与编排链路；不能让外部插件协议直接渗透进核心运行时。
- 来源：用户在确认可以继续推进插件适配层时，明确说明“外部插件再进来，也得先被压成受约束的 7Flows IR”。
- 影响范围：插件适配层、`api/` 运行时与校验、未来 compat adapter 服务、插件目录同步、调用代理与发布映射。
- 落地动作：已记录到 `docs/dev/user-preferences.md`；后续实现插件适配层时，将优先补齐“外部 manifest/descriptor -> constrained 7Flows IR” 的归一化边界与校验，而不是直接透传外部协议对象。
- 备注：这条偏好是“IR 优先与兼容层旁挂”的实现级细化，重点强调适配层入口必须做约束收口。

## 2026-03-10 每轮开发结束都要给出优先级规划并同步 runtime-foundation

- 偏好：每次完成一轮开发后，都要基于当前项目情况给出下一步开发建议，并按明确优先级列出顺序；同时必须更新相关开发文档，并把下一步规划同步写入 `docs/dev/runtime-foundation.md`。
- 来源：用户在 workflow 编辑器骨架落地后明确指出，希望把“优先级化下一步计划 + 文档同步”提升为长期协作约束。
- 影响范围：所有开发任务的收尾方式、`AGENTS.md`、`docs/dev/runtime-foundation.md`、`docs/dev/` 各专题记录，以及最终对用户的汇报结构。
- 落地动作：已将该要求补充到仓库级 `AGENTS.md`；本轮同步更新 `docs/dev/runtime-foundation.md`，把当前项目事实与下一步优先级计划写入固定落点。
- 备注：这条偏好强调“开发闭环”不仅包括代码、测试和提交，也包括面向下一轮的可执行优先级规划。

## 2026-03-11 基础框架要围绕主业务推进

- 偏好：项目不能长期只沿着基础框架、底座能力和内部抽象单线深化；后续开发要始终结合主业务推进，优先围绕四条业务主线统筹：`应用新建编排`、`编排节点能力`、`Dify 插件兼容`、`API 调用开放`。
- 来源：用户在确认“近期改动是否属于基础框架”后，明确要求调整后续开发偏好，不要一直深化框架和基础主线。
- 影响范围：`docs/dev/runtime-foundation.md`、后续开发优先级排序、产品路线拆分、架构设计与实现取舍，以及最终开发汇报。
- 落地动作：已记录到 `docs/dev/user-preferences.md`；同步调整 `docs/dev/runtime-foundation.md` 的下一步优先级，使基础能力建设改为服务主业务闭环，而不是独立成为长期主线。
- 备注：架构仍要先设计好，但目标是支撑“一切皆插件”的交互模型，以及运行时的稳定执行、可扩展性、可维护性、鲁棒性与可用性，而不是为了抽象而抽象。

## 2026-03-11 功能完整后联系人工全链路测试

- 偏好：当项目达到“主要功能完整、功能流程没问题、数据流转没问题、界面功能没有明显问题”的阶段后，应主动联系用户进行一次人工全链路完整测试，而不是默认只停留在自动验证或内部判断。
- 来源：用户在本轮任务中明确要求，待项目整体完整度达到可人工验收阶段时再联系其进行完整链路测试。
- 影响范围：后续开发收尾标准、阶段性验收节奏、`docs/dev/runtime-foundation.md` 的下一步规划，以及最终汇报中的测试建议。
- 落地动作：已记录到 `docs/dev/user-preferences.md`；后续在项目进入较完整阶段时，会把“联系用户进行人工全链路测试”作为显式收尾动作写入开发建议，而不是只给内部技术验证结论。
- 备注：在尚未达到“主要功能与界面较完整”的阶段前，仍优先用自动化验证和开发记录推进，不提前打扰用户做全链路人工验收。

## 2026-03-11 Agent Node 必须保持单一主控与证据分层

- 偏好：AI 节点要演进为节点内可恢复的 phase pipeline，但主 AI 仍是唯一决策者；assistant 只做工具结果整理、压缩和 evidence 提炼，不能负责流程推进，也不应默认直接吞入全量原始工具结果。
- 来源：用户在本轮“Durable Agent Workflow Runtime”系统级重构要求中明确提出，希望将 7Flows 长期演进到“流程级确定性 + 节点级智能性 + 上下文分层 + Durable Execution”的形态。
- 影响范围：`AGENTS.md`、`docs/dev/runtime-foundation.md`、运行时模型、Agent Runtime、Tool Gateway、上下文服务、后续执行视图与证据视图设计。
- 落地动作：已在 `AGENTS.md` 增补仓库级架构边界；本轮已实现 phase state machine 风格的 `llm_agent`、artifact/evidence 分层、Tool Gateway 与 resume API，并补充开发记录说明 Phase 1 MVP 与 Phase 2 演进路径。
- 备注：assistant 必须保持可关闭；关闭时应退化兼容旧的单次 AI 节点逻辑。
