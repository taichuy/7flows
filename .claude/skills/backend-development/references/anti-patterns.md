# Backend Anti-Patterns

| 反模式 | 风险 | 替代方式 |
| --- | --- | --- |
| 协议泄漏进核心 | 外部变化直接污染业务规则 | 在适配层做映射，核心只接受稳定语义 |
| 多入口写同一状态 | 状态来源不可追踪 | 收敛到唯一入口或统一动作服务 |
| 超大接口 | 调用难、校验难、演化难 | 拆成短、平、单动作接口 |
| 隐式副作用写状态 | 行为不可预测 | 显式声明动作和写路径 |
| service/repository/handler 三层都能改模型 | 变化传播失控 | 明确写权限边界和入口职责 |
